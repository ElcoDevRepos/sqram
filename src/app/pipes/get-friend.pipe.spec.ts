import { GetFriendPipe } from './get-friend.pipe';

describe('GetFriendPipe', () => {
  it('create an instance', () => {
    const pipe = new GetFriendPipe();
    expect(pipe).toBeTruthy();
  });
});
